dist: trusty
sudo: false
language: ruby
rvm:
  - 2.5
  - 2.4
  - 2.3
  - ruby-head
before_install:
  - gem install bundler -v 2.0.1
  - bundle update
  - unset _JAVA_OPTIONS
matrix:
  allow_failures:
    - rvm: ruby-head
